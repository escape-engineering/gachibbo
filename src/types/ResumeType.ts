export interface EducationType {
  edu_id?: string;
  graduated_at: string;
  school_name: string;
  major: string;
}
export interface ExperienceType {
  exp_id?: string;
  exp_period: string;
  exp_region: string;
  exp_position: string;
  exp_desc: string;
}
export interface LicenseType {
  lic_id?: string;
  lic_date: string;
  lic_title: string;
  lic_agency: string;
}
